[package]
name = "ex-musl"
version = "0.1.0"
edition = "2024"
description = "Minimal exit-only binary using musl (static linking)"

[dependencies]

[dev-dependencies]
is-libc-used = { path = "../../tools/is-libc-used" }
