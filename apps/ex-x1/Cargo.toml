[package]
name = "ex-x1"
version = "0.1.0"
edition = "2024"
description = "Minimal exit-only binary using rlibc-x1"

[dependencies]
rlibc-x1 = { path = "../../libs/rlibc-x1" }

[[bin]]
name = "ex-x1"
test = false
