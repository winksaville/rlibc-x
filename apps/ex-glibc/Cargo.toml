[package]
name = "ex-glibc"
version = "0.1.0"
edition = "2024"
description = "Minimal exit-only binary using glibc (dynamic linking)"

[dependencies]

[dev-dependencies]
is-libc-used = { path = "../../tools/is-libc-used" }
